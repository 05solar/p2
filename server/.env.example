# 서버 포트 (프론트 빌드 결과물도 이 포트에서 함께 서빙됩니다)
PORT=4000

# JWT 서명 비밀키 — 운영 시 반드시 변경하세요
JWT_SECRET=change-me-in-production

# 토큰 만료 기간
JWT_EXPIRES=7d

# 데이터 저장 파일 경로 (기본: server/data/db.json)
# DATA_FILE=./data/db.json
